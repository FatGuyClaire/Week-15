import "./App.css";
import SongList from "./components/song-list";

function App() {
  
  // I placed the main componenet into the app, and kept the App.css as it centered text.
  return <SongList />;
}

export default App;
